# Demo extras. Install the integration (editable) + these into a venv:
#   uv venv .venv --python 3.13
#   uv pip install -e . -r examples/demos/requirements-demos.txt
# (psycopg + pgvector come in via lightrag-agensgraph itself.)
lightrag-hku>=1.5.3,<1.6
openai>=1.40
datasets>=2.19
huggingface-hub>=0.23
python-dotenv>=1.0
tqdm>=4.66
tiktoken>=0.7
